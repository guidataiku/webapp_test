import { AbstractControl, FormGroupDirective, NgForm } from '@angular/forms';
import { ErrorStateMatcher } from '../core';
export declare const updateErrorState: (errorState: boolean, parentFormGroup: FormGroupDirective | null, parentForm: NgForm | null, control: AbstractControl | null, matcher?: ErrorStateMatcher) => boolean;
export declare const isErrorState: (control: AbstractControl | null, form: FormGroupDirective | NgForm | null) => boolean;
