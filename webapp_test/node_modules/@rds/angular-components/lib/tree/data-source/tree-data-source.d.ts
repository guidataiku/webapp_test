import { CollectionViewer, DataSource } from '@angular/cdk/collections';
import { BehaviorSubject, Observable } from 'rxjs';
import { RdsTreeFlattener } from './tree-flattener';
import { RdsTreeControl } from './tree-control';
export declare class RdsTreeDataSource<T, F, K = F> extends DataSource<F> {
    private _treeControl;
    private _treeFlattener;
    _flattenedData: BehaviorSubject<F[]>;
    _expandedData: BehaviorSubject<F[]>;
    _data: BehaviorSubject<T[]>;
    get data(): T[];
    set data(value: T[]);
    constructor(_treeControl: RdsTreeControl<F, K>, _treeFlattener: RdsTreeFlattener<T, F, K>, initialData?: T[]);
    connect(collectionViewer: CollectionViewer): Observable<F[]>;
    disconnect(): void;
}
