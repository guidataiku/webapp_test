import { Observable } from 'rxjs';
import { OverlayRef } from '@angular/cdk/overlay';
import { RdsDialogContainerComponent } from '../dialog-container.component';
export declare class RdsDialogRef<T, D = any, R = any> {
    private readonly overlayRef;
    readonly dialogContainer: RdsDialogContainerComponent<D>;
    readonly id: string;
    private readonly afterOpened$;
    private readonly afterClosed$;
    private readonly beforeClosed$;
    private result;
    private dialogState;
    private closeFallbackTimeout;
    componentInstance: T | null;
    disableClose: boolean | undefined;
    constructor(overlayRef: OverlayRef, dialogContainer: RdsDialogContainerComponent<D>, id?: string);
    close(dialogResult?: R): void;
    afterOpened(): Observable<void>;
    afterClosed(): Observable<R | undefined>;
    beforeClosed(): Observable<R | undefined>;
    backdropClick(): Observable<MouseEvent>;
    centerDialog(): void;
    changeSize(width?: string, height?: string): void;
    updateOverlayPosition(): void;
    private finishDialogClose;
    private getPositionStrategy;
}
