import { Observable } from 'rxjs';
import { RdsToastRef } from './rds-toast.ref';
import { RdsToastIndividualConfig } from './rds-toast.config';
export declare class RdsToastPackage<T, A> {
    readonly toastId: number;
    readonly config: RdsToastIndividualConfig;
    readonly message: string | null | undefined;
    readonly title: string | undefined;
    readonly toastRef: RdsToastRef<T>;
    private readonly _onTap;
    private readonly _onAction;
    constructor(toastId: number, config: RdsToastIndividualConfig, message: string | null | undefined, title: string | undefined, toastRef: RdsToastRef<T>);
    triggerTap(): void;
    onTap(): Observable<void>;
    triggerAction(action: A): void;
    onAction(): Observable<A>;
}
