import { Observable } from 'rxjs';
import { RdsToastOverlayRef } from './overlay/rds-toast-overlay-ref';
export declare class RdsToastRef<T> {
    private readonly _rdsToastOverlayRef;
    componentInstance: T;
    private readonly _afterClosed;
    private readonly _activate;
    private readonly _manualClose;
    private _isClosed;
    constructor(_rdsToastOverlayRef: RdsToastOverlayRef<T>);
    manualClose(): void;
    manualClosed(): Observable<void>;
    close(): void;
    isClosed(): boolean;
    afterClosed(): Observable<void>;
    isInactive(): boolean;
    activate(): void;
    afterActivate(): Observable<void>;
}
