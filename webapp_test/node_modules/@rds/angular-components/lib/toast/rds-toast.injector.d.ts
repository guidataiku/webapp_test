import { AbstractType, InjectFlags, InjectionToken, InjectOptions, Injector, Type } from '@angular/core';
import { RdsToastPackage } from './rds-toast.package';
export declare class RdsToastInjector<T, A> implements Injector {
    private readonly _toastPackage;
    private readonly _parentInjector;
    constructor(_toastPackage: RdsToastPackage<T, A>, _parentInjector: Injector);
    get(token: Type<T | RdsToastPackage<T, A>> | InjectionToken<T> | AbstractType<T>, notFoundValue: undefined, options?: InjectFlags | InjectOptions): T | RdsToastPackage<T, A> | null;
}
