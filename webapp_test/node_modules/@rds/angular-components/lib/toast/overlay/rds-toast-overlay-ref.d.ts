import { ComponentRef } from '@angular/core';
import { RdsBasePortalHost, RdsComponentPortal } from '../portal/rds-component-portal';
export declare class RdsToastOverlayRef<T> {
    private readonly _portalHost;
    constructor(_portalHost: RdsBasePortalHost<T>);
    attach(portal: RdsComponentPortal<T>, newestOnTop?: boolean): ComponentRef<T>;
    detach(): void;
}
